# QR Code Attendance System Bot

A modular Node.js Telegram bot that decodes QR codes from IITK ID card images, validates student registration, tracks attendance logs with file persistence, and provides administrative status reports.

## Setup Instructions

1. **Install Dependencies**
   Navigate into the `assignment2` folder and install required runtime modules:
```bash
   npm install
